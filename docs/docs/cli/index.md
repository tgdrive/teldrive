# CLI

These pages are generated from the Cobra command tree and config-backed flags in this repository.

## Commands

- [`check`](/docs/cli/check) — Check and purge incomplete files in Telegram channels
- [`run`](/docs/cli/run) — Start Teldrive Server
- [`version`](/docs/cli/version) — Check the version info

> Re-run `task gen:docs` after changing commands, defaults, or flag descriptions.
